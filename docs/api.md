# API meteo

In questo progetto i dati meteo sono pensati per arrivare da un servizio esterno, come OpenWeatherMap o Open-Meteo.

L'idea è mostrare informazioni semplici come temperatura, vento e condizioni del cielo. Per ora è una versione di esempio e i dati non vengono ancora letti da un'API in tempo reale.
